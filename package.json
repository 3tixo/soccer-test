{
  "name": "pitchpulse-ios-wrapper",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor iOS wrapper for the PitchPulse hosted soccer app.",
  "scripts": {
    "cap:sync": "cap sync ios",
    "cap:open": "cap open ios",
    "cap:copy": "cap copy ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
